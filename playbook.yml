- name: Deploy and Configure Sim-to-MoTeC Telemetry Appliance
  hosts: motec_pi
  become: true
  vars_files:
    - config.yml
  roles:
    - role: common
    - role: samba
    - role: sim_to_motec
    - role: webapp
